---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: exchanges.conduit.mnke.org
spec:
  group: conduit.mnke.org
  names:
    kind: Exchange
    listKind: ExchangeList
    plural: exchanges
    shortNames:
    - ex
    singular: exchange
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.podName
      name: Pod
      type: string
    - jsonPath: .status.recovery.restartCount
      name: Restarts
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Exchange is the Schema for the exchanges API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ExchangeSpec defines the desired state of Exchange.
            properties:
              env:
                description: Env is a list of environment variables to set in the
                  container
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: Name of the environment variable. Must be a C_IDENTIFIER.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              image:
                description: Image is the container image for the workload
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: ImagePullPolicy describes when to pull the image
                enum:
                - Always
                - Never
                - IfNotPresent
                type: string
              imagePullSecrets:
                description: ImagePullSecrets is a list of references to secrets for
                  pulling images
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              recovery:
                description: Recovery defines the recovery behavior when the pod fails
                properties:
                  backoffLimit:
                    default: 3
                    description: BackoffLimit is the number of retries before applying
                      backoff
                    format: int32
                    type: integer
                  maxRestarts:
                    default: 5
                    description: MaxRestarts is the maximum number of times to restart
                      a failed workload
                    format: int32
                    type: integer
                type: object
              resources:
                description: Resources defines the compute resource requirements
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              serviceAccountName:
                description: ServiceAccountName is the name of the ServiceAccount
                  to use for the pod
                type: string
              stream:
                description: Stream defines the NATS JetStream configuration for this
                  Exchange
                properties:
                  retention:
                    description: Retention defines how long messages are kept in the
                      stream
                    properties:
                      maxAge:
                        default: 168h
                        description: MaxAge is the maximum age of messages to retain
                          (e.g., "168h" for 7 days)
                        type: string
                      maxBytes:
                        anyOf:
                        - type: integer
                        - type: string
                        default: 1Gi
                        description: MaxBytes is the maximum total size of messages
                          to retain
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      maxMessages:
                        default: 100000
                        description: MaxMessages is the maximum number of messages
                          to retain
                        format: int64
                        type: integer
                    type: object
                  subjects:
                    description: |-
                      Subjects defines the NATS subjects for this exchange
                      If not specified, defaults will be used based on Exchange ID
                    properties:
                      control:
                        description: |-
                          Control is the subject pattern for control messages
                          Defaults to "exchange.{exchange-id}.control"
                        type: string
                      input:
                        description: |-
                          Input is the subject pattern for messages TO the application
                          Defaults to "exchange.{exchange-id}.input"
                        type: string
                      output:
                        description: |-
                          Output is the subject pattern for messages FROM the application
                          Defaults to "exchange.{exchange-id}.output"
                        type: string
                    type: object
                type: object
            required:
            - image
            type: object
          status:
            description: ExchangeStatus defines the observed state of Exchange.
            properties:
              archival:
                description: Archival tracks archival status (for external archival
                  systems)
                properties:
                  archivedAt:
                    description: ArchivedAt is when the exchange was archived
                    format: date-time
                    type: string
                  externalReference:
                    description: ExternalReference is a reference to the archived
                      data (e.g., S3 path)
                    type: string
                  lastArchivedSeq:
                    description: LastArchivedSeq is the sequence number of the last
                      archived message
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the Exchange's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              consumerName:
                description: ConsumerName is the name of the durable NATS consumer
                type: string
              messageStats:
                description: MessageStats tracks message counts and sequences
                properties:
                  lastInputSeq:
                    description: LastInputSeq is the sequence number of the last input
                      message
                    format: int64
                    type: integer
                  lastOutputSeq:
                    description: LastOutputSeq is the sequence number of the last
                      output message
                    format: int64
                    type: integer
                  lastProcessedSeq:
                    description: LastProcessedSeq is the sequence number of the last
                      processed message
                    format: int64
                    type: integer
                  totalInput:
                    description: TotalInput is the total number of input messages
                    format: int64
                    type: integer
                  totalOutput:
                    description: TotalOutput is the total number of output messages
                    format: int64
                    type: integer
                type: object
              phase:
                description: Phase is the current phase of the Exchange
                enum:
                - Created
                - Pending
                - Running
                - Recovering
                - Completed
                - Failed
                - Archived
                type: string
              podName:
                description: PodName is the name of the current or last workload pod
                type: string
              podPhase:
                description: PodPhase is the phase of the workload pod
                type: string
              recovery:
                description: Recovery tracks recovery attempts and status
                properties:
                  lastRecoveryDuration:
                    description: LastRecoveryDuration is how long the last recovery
                      took
                    type: string
                  lastRestartTime:
                    description: LastRestartTime is when the workload was last restarted
                    format: date-time
                    type: string
                  restartCount:
                    description: RestartCount is the number of times the workload
                      has been restarted
                    format: int32
                    type: integer
                type: object
              streamName:
                description: StreamName is the name of the NATS JetStream stream
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
